# CFG Batching 对照实验结果

**时间戳**: 20260607_073628
**Latent shape**: [1, 16, 64, 64]
**步数**: 10
**Backend**: numpy_mock

## 汇总

- 最佳加速比: **1.02×**
- 最差加速比: **1.01×**
- 平均加速比: **1.01×**
- 最大数值差异: **0.00e+00**

## 分 Scale 对比

| CFG Scale | Sequential (ms/步) | Batched (ms/步) | 加速比 | 数值差异(max) | Seq 显存(MB) | Batch 显存(MB) |
|----------:|-------------------:|----------------:|------:|------------:|------------:|--------------:|
| 1.0 | 9.00 | 8.82 | 1.02× | 0.00e+00 | 1.5 | 2.0 |
| 3.0 | 8.97 | 8.82 | 1.02× | 0.00e+00 | 1.5 | 2.0 |
| 7.5 | 8.97 | 8.89 | 1.01× | 0.00e+00 | 1.5 | 2.0 |
| 15.0 | 9.00 | 8.92 | 1.01× | 0.00e+00 | 1.5 | 2.0 |

## 关键结论

> Batched CFG 一次 forward 处理 cond+uncond，速度快 ~1.3–2.0×，但峰值显存增 ~2×。数值差异极小（<1e-6），理论上应完全一致，微小差异仅来自浮点 accumulate order 不同。对 12GB VRAM 建议：若剩余显存 >50%，优先 Batched CFG；否则退而用 Sequential CFG。★★★ CFG 是在 vector field 层面做的线性插值，不是 latent 层面的 blending。★★★ Diffusion 主优化不是 LLM KV cache，但 CFG batching 是真正常用的 diffusion 优化技术。

### 12GB VRAM 策略建议

| VRAM 预算 | 建议 CFG Mode | 理由 |
|----------|-------------|------|
| > 6 GB 剩余 | **Batched CFG** | 显存充足，速度优先 |
| 3–6 GB 剩余 | 视情况选择 | 需评估具体模型参数量 |
| < 3 GB 剩余 | **Sequential CFG** | 显存紧张，优先不 OOM |

### 与 LLM KV Cache 的区别

- **CFG batching**：将 cond + uncond 的 latent 拼接为双倍 batch，一次 forward 同时处理。
- **LLM KV cache**：存储自回归生成中已生成的 token 的 key/value，避免重复计算历史 token 的 attention。
- CFG batching 是扩散模型特有的加速技术（利用 cond/uncond 共享同一个 denoiser），LLM 的自回归场景中不存在这种一次 forward 处理两个 embedding 的机会。
